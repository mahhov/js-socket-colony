{
  "name": "jssocket",
  "version": "1.0.0",
  "description": "",
  "web": "node ./out/socketServer.js",
  "main": "./out/socketServer.js",
  "scripts": {
    "build": "rm -rf out; cp -r src out; browserify src/socketClient.js -o out/socketClient.js; inline-scripts out/socketClient.html out/socketClient.html",
    "start": "node ./out/socketServer.js",
    "server-windows": "start cmd '/k node out/socketServer'",
    "dev-windows": "npm run build; npm run server-windows"
  },
  "author": "mahhov1",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.3",
    "inline-scripts": "0.0.1"
  },
  "dependencies": {
    "ws": "^6.1.3"
  },
  "engines": {
    "node": "11.x"
  }
}
